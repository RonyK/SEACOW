#include <pch.h>
#include <array/array.h>

namespace msdb
{
TEST(arrayBase, arrayCreate)
{
	////////////////////////////////
	//// Set up array
	//pDimensionDesc dimDesc = std::make_shared<dimensionDesc>();
	//dimDesc->id_ = 0;
	//dimDesc->name_ = "x";
	//dimDesc->start_ = 0;
	//dimDesc->end_ = 8;
	//dimDesc->chunkSize_ = 8;

	//pAttributeDesc attrDesc = std::make_shared<attributeDesc>();
	//attrDesc->id_ = 0;
	//attrDesc->name_ = "v1";
	//attrDesc->type_ = eleType::DOUBLE;

	//dimensionDescs dimDescs;
	//dimDescs.push_back(dimDesc);
	//attributeDescs attrDescs;
	//attrDescs.push_back(attrDesc);

	//pArrayDesc arrDesc = std::make_shared<arrayDesc>(0, "wavelet_test", dimDescs, attrDescs);

	//pArray sourceArr = std::make_shared<arrayBase>(arrDesc);
}
}
